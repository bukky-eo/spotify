import 'package:flutter/material.dart';

class IntroMusic extends StatelessWidget {
  const IntroMusic({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
          child: Column(
        children: [],
      )),
    );
  }
}
